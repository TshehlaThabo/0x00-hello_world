#include <stdio.h>
#include <stdlib.h>

/**
 * main - lowercase alphabets reverse
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char a;

	for ( a = 'z'; a >= 'z'; a--)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
